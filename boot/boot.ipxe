#!ipxe

set BASEURL http://private:_TOKEN_@10.0.1.1/coreos

dhcp

kernel ${BASEURL}/coreos.kernel initrd=main coreos.live.rootfs_url=${BASEURL}/coreos.rootfs ignition.firstboot ignition.platform.id=metal
initrd --name main ${BASEURL}/coreos.initrd

boot
