apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: nitter
configMapGenerator:
- name: config
  files: [nitter.conf]
  options: {disableNameSuffixHash: true}
resources:
- ../config
- helm-release.yaml
- namespace.yaml
- redis.yaml
