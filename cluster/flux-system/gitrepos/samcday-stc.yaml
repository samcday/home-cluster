apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: samcday-stc
  namespace: flux-system
spec:
  interval: 1h
  ref:
    branch: main
  # NOT code.samcday.com since it depends on stc for storage
  url: https://github.com/samcday/stc
