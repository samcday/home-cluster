apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: backend
  namespace: postgres
spec:
  commonName: backend
  duration: 8760h # 1 year
  issuerRef:
    name: ca
    kind: Issuer
  secretName: backend-cert
  usages: [server auth]
