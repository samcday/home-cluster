---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: servicelb-standalone
  namespace: kube-system
spec:
  chart:
    spec:
      chart: servicelb-standalone
      sourceRef:
        kind: HelmRepository
        name: samcday-servicelb-standalone
        namespace: flux-system
      version: 0.0.2
  interval: 1h
  values:
    # https://github.com/samcday/servicelb-standalone/blob/main/chart/values.yaml
