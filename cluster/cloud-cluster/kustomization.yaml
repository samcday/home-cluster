apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: control-plane-values
    files:
      - values.yaml=cluster-plane-values.yaml
resources:
  - ../config
  - cilium.yaml
  - cluster-autoscaler.yaml
  - coredns.yaml
  - hccm.yaml
  - helm-release.yaml
  - namespace.yaml
  - tofu.yaml
