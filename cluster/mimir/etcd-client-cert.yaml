apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: etcd-client
  namespace: mimir
spec:
  secretName: etcd-client-cert
  duration: 24h
  usages: [client auth]
  commonName: mimir
  issuerRef:
    name: etcd
    kind: ClusterIssuer
