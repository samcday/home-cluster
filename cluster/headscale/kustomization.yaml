apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: headscale
configMapGenerator:
  - name: config
    files:
      - config.yaml
resources:
  - ../config
  - helm-release.yaml
  - namespace.yaml
  - postgres.yaml
  - pvc.yaml
