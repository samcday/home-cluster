acls:
  - action: accept
    src: [sam]
    dst:
      - "*:*"
  - action: accept
    src: [cloud-cluster]
    dst:
      - cloud-cluster:*
      - sam:6443
      - tag:subnet-router:0
autoApprovers:
  routes:
    10.0.1.0/24: [tag:subnet-router]
