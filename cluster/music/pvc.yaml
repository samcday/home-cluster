apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: music
  namespace: music
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 100Gi
  storageClassName: ceph-filesystem-ec
