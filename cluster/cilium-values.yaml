# https://github.com/cilium/cilium/blob/master/install/kubernetes/cilium/values.yaml

ipam:
  mode: kubernetes
k8sServiceHost: 172.29.0.1
k8sServicePort: 6444
kubeProxyReplacement: strict
loadBalancer:
  serviceTopology: true
operator:
  rollOutPods: true
  replicas: 1

# this fails sporadically because Cilium tries to use tailscale0
# for auto direct node routes. Sigh.
# autoDirectNodeRoutes: true
# devices: enp+
# ipv4NativeRoutingCIDR: 172.30.0.0/16
# routingMode: native
# tunnel: disabled
