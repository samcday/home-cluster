# Mount encrypted external storage to /mnt/storage
# Relies on /etc/hotplug.d/block/extstorage-setup

set -uexo pipefail

uci -q batch <<EOI
set fstab.extstorage=mount
set fstab.extstorage.target='/mnt/storage'
set fstab.extstorage.device='dm-0'
commit fstab
EOI
